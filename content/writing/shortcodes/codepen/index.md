+++
title = "CodePen"
weight = 1
taxonomies = { tags = ["shortcodes", "codepen", "embed"], categories = ["shortcodes"] }
+++

`codepen`

- `codepen`: Embeds a CodePen pen. It takes `user` and `pen_id` as required parameters, and optionally `default_tab` (defaults to `result`).

```jinja
{{/* codepen(user="hahwul", pen_id="ByKwxwd") */}}
```

{{ codepen(user="hahwul", pen_id="ByKwxwd") }}
